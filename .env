VITE_API_BASE_URL=https://ecommerce-api-dev-1063728289659.us-central1.run.app/api/v1

# Production domain — used to build absolute URLs in sitemap.xml, robots.txt, and
# canonical/OG tags at build time. This is a placeholder (.example is IANA-reserved for
# documentation use); replace with the real deployed domain before going live.
VITE_SITE_URL=https://www.heardtastic.example
